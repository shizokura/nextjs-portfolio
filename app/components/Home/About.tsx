import aboutStyle from './About.module.scss';

const About = () => (
  <div className={ aboutStyle.container }>
    <div>
        <h2 className={ aboutStyle.title }>About Me</h2>
    </div>
  </div>
);

export default About;
